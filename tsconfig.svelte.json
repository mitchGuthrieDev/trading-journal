{
  "//": "svelte-check config (A77/A30). Extends the strict base and points svelte-check at the Svelte SPA (src/app/**: the .svelte components AND the .ts glue/entry that imports them). svelte-check understands .svelte and resolves the verbatim core imports from src/lib/.",
  "extends": "./tsconfig.json",
  "include": ["src/app/**/*.svelte", "src/app/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}

{
  "schemaVersion": 1,
  "title": "Blotterbook — completed backlog archive (Done items)",
  "intro": "Archived Done items moved out of backlog.json to keep the active backlog lean. Each retains its doneNote — the shipped record of what was actually done (prompts were already stripped per donePolicy). Newest work tends toward the end, mirroring the order items were completed in the live file.",
  "updated": "2026-06-27",
  "note": "Append-only archive of completed backlog items. When an item in backlog.json is set to `done`, move its object here. Admin-only and fetched no-store; like backlog.json it is EXCLUDED from the cache-busting manifest (build-manifest.mjs) and from version-bump path classification (bump-version.mjs), so editing it never churns the manifest or bumps the version. The admin Backlog panel fetches this file alongside backlog.json and merges the item lists.",
  "categories": [
    "SECURITY",
    "ARCHITECTURE / DUPLICATION",
    "LAYOUT (web vs mobile)",
    "COMMENTS / READABILITY",
    "FEATURES",
    "FIXES",
    "REVIEW / AUDIT",
    "CHORE / REFACTOR"
  ],
  "items": [
    {
      "id": "S1",
      "title": "Close the CSV→HTML injection (XSS)",
      "category": "SECURITY",
      "priority": "P1",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** `rootSym()` (`app/adapters.js:52`) now strips to `[A-Z0-9._-]` at the source, and `esc()` was applied at every sink (`app/app.js` cost table ~493, exported report ~1165, data-manager rows ~1385/1388, trade editor ~1421 — incl. `side`). Verified in-browser: `rootSym('MES<img …>')` → `MESIMGSRCXONERRORALERT1`; a stored payload renders as escaped text (no injected element, XSS never fired); legit symbols (MES/MNQ/MCL) still render; 55/55 adapter tests pass."
    },
    {
      "id": "S2",
      "title": "Add a Pages `_headers` file with CSP + security headers",
      "category": "SECURITY",
      "priority": "P1",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** root `_headers` file applies to `/*`: CSP (`default-src 'self'`; `img-src 'self' data:` for screenshots; `connect-src 'self'`; `object-src 'none'`; `base-uri 'self'`; `form-action 'self'`; `frame-ancestors 'none'`; no `'unsafe-eval'`), plus `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (camera/mic/geo/ topics off). `script-src`/`style-src` keep `'unsafe-inline'` (3 first-party inline scripts on home/ admin/changelog + inline `style=\"\"` attrs; no build step for nonces yet — tighten via A1/A2). NOTE: `_headers` only takes effect on Cloudflare Pages, not the local static preview — confirm the response headers on the next deploy. App/demo/staging/home/admin all still render."
    },
    {
      "id": "S3",
      "title": "Replace the raw-ADMIN_KEY-to-browser flow with a short-lived signed token",
      "category": "SECURITY",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** new `functions/_lib/auth.js` issues/verifies HMAC-SHA256 tokens. `/api/admin-key` now returns a short-lived signed token (TTL `ADMIN_TOKEN_TTL_SEC`, default 2h; signed with `TOKEN_SECRET`→ falls back to `ADMIN_KEY`) instead of the raw key — the raw `ADMIN_KEY` never reaches the browser. The admin page stores/sends the token (x-admin-key header for writes; `bb_staging` cookie for staging launch). `/api/status`, `/api/config`, and the `_middleware` staging gate verify it via `isAdminAuthorized()`; the raw key stays accepted server-side as a fallback (never handed to a browser). Tokens expire. Verified with a Node WebCrypto test (valid/tamper/expiry/fallback). Full E2E needs the deployed Access+KV env."
    },
    {
      "id": "S4",
      "title": "Verify the Cloudflare Access JWT signature (defense-in-depth)",
      "category": "SECURITY",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** `/api/admin-key` verifies the `Cf-Access-Jwt-Assertion` RS256 signature against the team JWKS (`<ACCESS_TEAM_DOMAIN>/cdn-cgi/access/certs`, cached 1h) plus audience (`ACCESS_AUD`), issuer, and expiry before issuing a token (`verifyAccessJwt` in `functions/_lib/auth.js`). When `ACCESS_TEAM_DOMAIN`/`ACCESS_AUD` are unset it falls back to the prior header-presence check (route is still behind Access + the middleware gate). Verified with a Node WebCrypto test using a generated RSA keypair + mocked JWKS: a genuine token is accepted; forged-signature, absent, expired, wrong-aud, wrong-iss, and unknown-kid are all rejected. Full E2E needs the deployed Access env."
    },
    {
      "id": "S5",
      "title": "Cap per-trade screenshot size",
      "category": "SECURITY",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** `dmAddShot` (`app/datamanager.js`) now auto-downscales (longest side ≤ 1600px) and re-encodes to JPEG q0.82 whenever an image exceeds a ~600 KB soft cap, flattening transparency first; images already compact are stored as-is, and if re-encoding doesn't shrink it the original is kept. Replaced the old \"over 4 MB, store anyway?\" confirm with a friendly post-add note. Verified in-browser: a 4.7 MB / 3000×2000 PNG → 1600px / ~73 KB JPEG, \"Image optimized for local storage (4.7 MB → ~73 KB)\" shown, 0 console errors."
    },
    {
      "id": "S6",
      "title": "Round out `.gitignore`",
      "category": "SECURITY",
      "priority": "P3",
      "effort": "trivial",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** added `.mf/` (under Cloudflare), `npm-debug.log*` + `pnpm-debug.log*` (under logs), and a new \"Local-only overrides & caches\" section with `*.local` + `.cache/`. No existing entries removed."
    },
    {
      "id": "A1",
      "title": "Single-source nav, footer, app-shell, and design tokens via a tiny prebuild",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P2",
      "effort": "large",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** `tokens.css` is the lone source of design tokens — `site.css` + `app/app.css` `@import` it, the homepage links it (3 `:root` blocks → 1; verified `--accent`/bg resolve on info, home, and app pages). `partials/nav.html` + `partials/footer.html` are injected by `scripts/build-includes.mjs` (idempotent; `active=KEY` highlights the current link) into the four info pages via `<!-- include:nav/footer -->` markers. Footers were unified (each had drifted — self-link omission + a howto-only line, which was relocated into howto's body). Homepage + admin keep bespoke nav/footer by design (different links/CTA/scroll-spy), sharing only tokens. Verified in-browser: nav active states, unified footer, correct theme, 0 console errors; build re-run reports 0 changes. **Not done (deferred):** the app *shell* (`app/index.html`↔`staging.html` near-duplicate markup) is still hand-kept — fold into a shared shell partial in a follow-up (pairs naturally with A3)."
    },
    {
      "id": "A2",
      "title": "Split `app/app.js` into concern-scoped script files",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** `app.js` → 7 ordered classic scripts: `core.js` (globals/helpers/metrics/format/cost/ refdata), `render.js` (cards/curve/calendar/advanced/break-even + scope/filter driver), `data.js` (import/demo/filters/journal/session/setup), `ui.js` (panels/staging flair/file-dl), `export.js` (report), `datamanager.js` (manage-data modal), `main.js` (event wiring + boot, loaded LAST). Cut at section boundaries in original order, so concatenation == original (no reordering). Verified: all 7 pass `node --check`; main app (initial state, 9 brokers), demo (loaded, curve), and staging (v0.13, 1539-row manage data, terminal, session pill) all boot with 0 console errors; cross-file fns resolve. Old app.js removed; 3 HTML pages load the 7 scripts in order. README updated. Note: staging code still lives in `ui.js` — fully isolating it is A3."
    },
    {
      "id": "A3",
      "title": "Move staging-only JS into its own file + decouple from shared code",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** all staging flair (activity terminal, session pill, workspace templates, the staging resize redraw) moved from `ui.js` into a new `app/staging.js` that ONLY `staging.html` loads (inserted before `main.js`). `core.js` gained a tiny event bus (`emit`/`onEvent` over `EventTarget`); the `logAction()` calls in shared code (`data.js`, `datamanager.js`, `main.js`) were replaced with `emit('data:imported' | 'note:saved' | 'trade:deleted' | 'backup:created' | 'data:erased')`, and `boot()` now `emit('app:ready')` instead of calling `initStaging()`. `staging.js` subscribes and logs. `seedStagingIfEmpty` stays in `main.js` (boot-time data bootstrap; keeps the boot path robust). Verified in-browser: main app + demo load NO staging.js and define zero staging fns (boot fine, 0 console errors); staging's terminal logs session-ready + backup + delete via the bus. All 8 scripts pass `node --check`. Note: the staging↔index app-shell markup duplication (deferred from A1) is still open — now tracked as A5."
    },
    {
      "id": "A5",
      "title": "Single-source the app shell (staging.html ↔ app/index.html ↔ demo.html)",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P2",
      "effort": "large",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": true,
      "doneNote": "**Shipped (partial):** extended `scripts/build-includes.mjs` to inject shared app-shell partials into all three app pages via a tiny `<!--IF mode=…-->` conditional. Single-sourced into `partials/`: `app-landing.html` (app+staging), `app-scope.html` (all three), `app-filters.html` (+staging saved-filter controls via flag), `app-dash.html` (the five dashboard panels; demo journal-hint via flag), `app-modal.html` (data-manager modal; staging Saved-filters section via flag). Editing a partial + re-running the build updates all three pages. The staging-only Activity Terminal stays inline right after the shared dash include — it exists on staging ALONE, so it was never duplicated. Verified: each built page's id/data-key attribute set is identical to before; demo carries none of the staging-only nodes; the build is idempotent; all three boot in Chromium with 0 page errors (app=5 panels, demo=5, staging=6 incl. terminal + saved-filter controls). **Deferred:** the three top bars stay per-page (version badge, button sets, staging session-pill/workspace-bar diverge too much to fold safely), and demo keeps its bespoke trimmed landing + preview-only data manager."
    },
    {
      "id": "L1",
      "title": "Consolidate ad-hoc breakpoints into a named scale",
      "category": "LAYOUT (web vs mobile)",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** consolidated the ad-hoc media breakpoints into a documented named scale — xs 460 · sm 560 · cal 620 · md 760 · lg 900, plus the wide min-width tiers 1100/1480/1700/1920 (full table commented at the top of `app/app.css`; one-line pointers in `site.css` + `index.html`). CSS media queries can't read custom properties without a build step, so the scale is a documented convention + snapped literals. Snapped the near-duplicates: `.cards` & `.mcards` 520→560, nav-hamburger 720→760, `.grid3` & `.price-grid` 880→900. `@media` conditions now use only {460,560,620,760,900} + the wide tiers — 520/720/880 no longer appear as breakpoints. Responsive-in-place kept (no separate mobile files). Verified in Chromium at the boundary widths (home 1280/900/560, app 560, howto 760 hamburger) — no regressions."
    },
    {
      "id": "L2",
      "title": "Use wide-display real estate deliberately (4K)",
      "category": "LAYOUT (web vs mobile)",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": true,
      "doneNote": "**Shipped:** main app + demo `.wrap` now bumps to 1360px (≥1480) / 1520px (≥1920) — dashboard fills wide displays (verified composed at 1920px, side margins ~200px vs ~370px). **Deliberately did NOT widen the marketing homepage**: bumping its container pushed body paragraphs to ~1520px (~150 chars/line), hurting line-length. Possible follow-up: widen only the homepage's grid sections (features/pricing), leaving prose narrow. On very wide screens the marketing pages and main app cap narrow while staging goes edge-to-edge. Review the max-width caps and make the main app + key marketing sections use the extra width more intentionally on ≥1480px (without hurting readability/line length). Done when: at 4K the main app and homepage feel composed, not stranded in a narrow column."
    },
    {
      "id": "L3",
      "title": "(staging) Move the session indicator next to the wordmark",
      "category": "LAYOUT (web vs mobile)",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** pill now sits after the \"Blotterbook v0.13\" version badge; wrapped pill + popup in a relatively-positioned `.sesswrap` so the legend popup stays anchored under the pill. Verified in-browser. In `app/staging.html` the session-status pill (`#sesspill`) sits at the far LEFT of the header, before the wordmark. Reposition it to the RIGHT of the \"Blotterbook v0.13\" text (after the `.ver` badge) so it reads \"Blotterbook v0.13 · ●Online\". Keep the click-to-explain popup working and the header left-aligned. Touch the `.brand` markup + `app.css`. Done when: the online/offline/degraded pill sits just right of the version badge, popup still opens, no mobile layout shift."
    },
    {
      "id": "L4",
      "title": "(staging) Move Reset filters next to Save filter",
      "category": "LAYOUT (web vs mobile)",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** Reset filters now sits immediately after Save filter (dropped `fbtn`/margin-left:auto); the trade-count floats to the right of the bar via a new `.fpush` util. Verified adjacent + count right. In the staging filter bar (`app/staging.html` + `app.css`), the Reset filters button (`#f_reset`) is separated from Save filter (`#f_save`). Relocate Reset so it sits adjacent to Save filter in the filter module. Both must keep working. Done when: Save filter and Reset filters are next to each other in the filter bar; filtering + saving still work."
    },
    {
      "id": "C1",
      "title": "Orient readers of the multi-surface app scripts",
      "category": "COMMENTS / READABILITY",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** added an orientation block at the top of `app/core.js` (7-script load order, `STAGING_PAGE`/ `DEMO_MODE` gating, shared-global-state map + \"persist only via Store\" note), and intent comments on the dense metrics math (realized max-drawdown walk, consecutive-streak loop, daily-PnL Sharpe) plus the equity-curve linear scales in `app/render.js`. Comments only — diff is 31 insertions/0 deletions; both files pass `node --check`."
    },
    {
      "id": "F1",
      "title": "Backlog module in the admin panel (migrate backlog.md → committed backlog.json)",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "large",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** migrated `.claude/backlog.md` → committed `data/backlog.json` (26 items, all fields incl. prompts + doneNotes) via a one-time parser, then retired the `.md` to a redirect stub. Added a read-only **Backlog** section to `admin.html` that fetches the JSON and renders per-category completed/remaining count cards + an overall tally + the grouped item list (id / title / effort / status badge). Verified in-browser: 8 categories, 26 rows, \"15 done · 10 remaining · 1 guardrail · 60% complete\", and that the prompt/doneNote text is NOT present in the rendered admin DOM. 0 console errors."
    },
    {
      "id": "F2",
      "title": "(staging) Workspace dropdown reverts to default; remove the Default button",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** placeholder renamed \"— Workspace —\" → \"— Default —\"; selecting it now clears the saved order/collapsed state and resets to `DEFAULT_DASH_ORDER`. Removed the standalone Default button. Verified: scrambled layout reverts on selecting \"— Default —\". Covers two related staging requests. (1) Rename the workspace-template `<select>` placeholder from \"— Workspace —\" to \"— Default —\", and make SELECTING it revert the layout to default (today the placeholder is a no-op; the separate Default button does the revert). (2) Remove the now-redundant `#ws_default` \"Default\" button. Touch `app/staging.html` + the staging UI handlers (`ui.js` `initStaging` / the `ws_*` wiring). Done when: choosing \"— Default —\" resets module order + collapsed state to default; the standalone Default button is gone; Save layout and loading saved templates still work."
    },
    {
      "id": "F3",
      "title": "(staging) Export-report popup: preview + multi-format download + email",
      "category": "FEATURES",
      "priority": "P2",
      "effort": "large",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped (staging-only):** Export report now opens an in-page modal (`#exportModal`) with a scrollable iframe preview + a format `<select>` (PDF print / PNG / JPEG / Markdown; Download disabled until a real format is picked) + Email-a-copy (mailto) + Close. PNG/JPEG rasterize the report via an SVG `<foreignObject>`→canvas (no new dependency); MD builds a markdown summary; PDF prints the iframe. `exportReport()` was refactored to share the report CSS/markup between the unchanged new-tab path (main/demo) and the modal. Verified in Chromium: preview renders, format gating works, MD + PNG downloads succeed, 0 errors; demo/main keep the new-tab export."
    },
    {
      "id": "B1",
      "title": "(staging) Edit/Delete buttons overlap the PnL column in the trades table",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** root cause was `display:flex` on a `<td>` (dropped it out of the table column model and overlapped PnL). Reverted to a normal table-cell; space the buttons with margin. Verified: action cell starts exactly at the PnL cell's right edge, no overlap. (Fix is in shared `app.css`, applies app-wide.) In the Manage-data trades table, a per-trade row's Edit/Delete action buttons visually overlay the PnL number (the actions render on top of the PnL column). Fix the table/cell layout in `app.css` (and the row markup in `app/datamanager.js` if needed) so the actions occupy their own column with no overlap. Check at staging fullscreen widths where it's most visible. Done when: PnL and the Edit/Delete actions are cleanly separated across widths; Edit/Delete still work."
    },
    {
      "id": "B2",
      "title": "(staging) Activity Terminal should fill its module (no negative space)",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** `.panel.term .pbody` is now a growing flex column and the terminal flexes to fill it (160px min on narrow). Verified at 1800px (3-col): terminal grows to 664px to fill its 777px panel. The Activity Terminal console (`#termwin`) has a fixed height, leaving empty space below it inside the panel on the wide/fullscreen staging grid. Make the terminal window fill its module body (flex / height:100%) so there's no gap, keeping the scroll + 200-line cap. `app.css` (`.panel.term` / `.termwin`). Done when: in fullscreen staging the terminal fills its panel with no negative space; scroll + autoscroll still work."
    },
    {
      "id": "R2",
      "title": "Roadmap rework: customer-facing, big-picture",
      "category": "REVIEW / AUDIT",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Rewrote roadmap.html as a customer-facing shipped/in-progress/planned feature story (it had become a stale internal-backlog mirror). Available now: 7 benefit-framed capabilities (multi-platform CSV auto-import, true cost+tax P&L, the edge-to-edge dashboard, drag/collapse/save-layout, filter+journal with tags/screenshots, downloadable/emailable reports, privacy/local + demo). In progress: trustworthy live rates [High priority], broader verified platform coverage, deeper journaling (R-multiple / MAE-MFE / setup tags). Planned: device sync [$5/mo], direct broker connections, per-trade chart replay [Stretch], power-user workspace. Dropped all engineering/infra minutiae (code-review pass, compliance session, Stripe wiring, versioned-releases infra, staging chips) and refreshed for everything shipped since it was written. Removed the strikethrough on shipped items in site.css so they read as available, not crossed-off tasks (.checks is roadmap-only). Verified in-browser: 3 sections (7/3/4 items), zero engineering-jargon leak, nav/footer/tokens intact, no errors."
    },
    {
      "id": "R3",
      "title": "(staging) Design review: per-day vs per-trade notes & data model",
      "category": "REVIEW / AUDIT",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Design review of how per-DAY and per-TRADE notes fit together. FINDINGS: per-day notes (JOURNAL store, {date,text,updated}) are plain text only, captured in a bare textarea under the calendar; per-trade notes (TRADEMETA store, {id,tags,note,shots,updated}) are rich — tags + note + downscaled screenshots — captured in a modal editor in Manage data. The asymmetry is backwards: the DAY is the natural home for a chart screenshot / morning plan / 'tilted' tag, yet only trades can hold them. Two storage shapes + two capture surfaces mean any future notes feature (search, export, tag filter) must be built twice. The 'note'-only Day-notes list in Manage data is ALREADY fixed — verified in-browser it previews date + full text (newest first); the only bare literal 'note' left is the compact per-trade presence chip (metaChips), which is intentional. RECOMMENDATION: one annotation shape {tags,text,shots}, two scopes (day|trade). Low-risk path = extend the JOURNAL record to {date,text,tags,shots,updated} (old {date,text} records read fine, fields default empty; additive export), reuse the trade editor's tag input + screenshot downscale pipeline, and replace the bare calendar textarea with a shared annotation editor (quick textarea that expands to full editor). Keep the CH11 capture-the-date autosave and the SHOT downscale pipeline. Spun off F16 (unify: give day notes tags + screenshots), CH19 (extract a shared annotation-editor component), CH20 (Manage-data Day-notes rows open/edit + consistent note·img·tag markers). No app code shipped — R3 is review-only."
    },
    {
      "id": "R4",
      "title": "Review \"Definitions & Caveats\" for accuracy",
      "category": "REVIEW / AUDIT",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-25",
      "partial": false,
      "doneNote": "**Shipped:** the only stale item was \"Trade = one row … exactly as TradingView exported it\" — rewrote it to \"Trade = one closed position\" covering auto-detection, one-row-per-close vs FIFO fills-pairing (+ hold time), and the beta caveat. Applied to all 3 app pages (identical panel). Rest verified accurate. Re-read the dashboard's \"Definitions & Caveats\" panel against the app's CURRENT behavior and correct anything stale — metric definitions, scope behavior, cost/tax model wording, and any staging-only features. Accuracy pass only (content). Done when: every definition/caveat matches current functionality; corrections applied."
    },
    {
      "id": "CH1",
      "title": "Rename `app/index.html` → `app/app.html`",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** `git mv app/index.html → app/app.html`, then added a root `_redirects` rewriting `/app/` → `/app/app.html` (200 = rewrite; the URL stays `/app/`) so the clean URL and every existing Launch link keep working with zero link churn. README updated (file-tree entry, `app.css` shared-by line, promote-staging note, and quick-start — incl. a note that the `/app/` rewrite is Pages-only, so the local static server should open `/app/app.html` directly). No code referenced `app/index.html` directly (the `../index.html` link is the homepage). Verified app.html boots in Chromium."
    },
    {
      "id": "F4",
      "title": "(staging) Keep the expanded Broker & Costs panel in view while scrolling",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped (staging-only):** the expanded Broker & Costs panel is now `position:sticky` while scrolling the dashboard — `display:contents` on `#landing` (when loaded) lifts `#setup` out of its short box so sticky has the full `.wrap` height. Collapsing the panel returns it to normal flow; below 760px it degrades to non-sticky so it never overlaps content. Gated by `body[data-mode=staging].loaded`. Verified in Chromium: position sticky when expanded, static when collapsed; demo/main unaffected (static)."
    },
    {
      "id": "F5",
      "title": "(staging) Rework app-state flow: initial state is always the entry point",
      "category": "FEATURES",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped (staging-only):** staging always boots on the initial/landing state. When the isolated DB has data, `restoreSession` calls `armStagingLanding()` instead of entering the dashboard: it swaps the headline to “Start Blotterbook to access your dashboard”, updates the blurb, shows a “✓ Dashboard data already loaded — N trades” note, and enables Start; Start then opens the existing dashboard via `enterStagingDashboard()`. With no data the normal load-CSV flow is unchanged (Start gated). Main app + demo untouched (verified: demo still boots straight to the dashboard)."
    },
    {
      "id": "F6",
      "title": "(staging) Make the Break-Even commission table a collapsible subsection",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped (staging-only):** the per-symbol commission table moved into a collapsible `<details class=\"csubtable\">` nested under the “Commissions (all-in)” line in the Break-even panel, styled to match the “Tax — Section 1256” subsection (`.csub` + the `.cdefs` ▸/▾ marker). Collapsed shows just the all-in total; expanded shows the breakdown. Built in `render.js` gated by `STAGING_PAGE` (the standalone `#c_comm_table` is left empty in staging). Verified in Chromium: collapsible present with the table inside, `#c_comm_table` empty; demo/main keep the standalone table."
    },
    {
      "id": "F7",
      "title": "(homepage) Merge Features + Use Cases into one redesigned Features section",
      "category": "FEATURES",
      "priority": "P2",
      "effort": "large",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the homepage Features section now leads with a horizontal row of four condensed use-cases, then a clickable feature LIST (left) + detail panel (right) that swaps on selection (first item pre-rendered for initial paint / no-JS). The standalone Use Cases section was removed and its pitch line folded in. Responsive: the use-case row collapses 4→2→1 and the explorer stacks to one column at md (760). Dropped the unused `.grid3/.fcard/.uc/.uc-grid` styles. Verified in Chromium: 4 use-cases, 6 feature items, click swaps detail + active state, single-column on mobile, 0 errors."
    },
    {
      "id": "F8",
      "title": "(admin) Strike through completed items in the backlog view",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "trivial",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** completed (status=done) rows in the admin Backlog list now render the title with `text-decoration:line-through`, visually distinct from open/guardrail items (which are unaffected). Verified in Chromium (computed `line-through` on done titles)."
    },
    {
      "id": "F9",
      "title": "(admin) Filter the backlog by status and effort",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** added Status and Effort `<select>` filters (options derived from the data) to the admin Backlog module; they combine and narrow the item LIST only — the per-category counts stay full project totals (noted inline). A Clear-filters button resets both. Verified in Chromium: 40 rows → status=done 21 → +effort=small 12 → clear 40, every shown row matching the active filter."
    },
    {
      "id": "R5",
      "title": "(staging) Review the Advanced Statistics module",
      "category": "REVIEW / AUDIT",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Reviewed the Advanced Statistics module and shipped the contained UI fix. UI: wrapped every stat value in a single `.avg` flex group (app/render.js + app/app.css) so rows with a secondary value (Long/Short PnL, Best/Worst Day, Best/Worst Weekday, Avg Hold) no longer strand the MAIN figure in the middle of the row — all 21 rows now right-align at the same edge (verified in-browser). WEEKDAY CALC documented in core.js: bestDow/worstDow bucket each trade by local weekday and pick the active weekday with the highest/lowest TOTAL PnL; noted the caveat that total (not per-trade) PnL skews toward heavily-traded weekdays and is noisy on small samples (the demo's 'worst weekday' is still +$14.9k), so it marginally earns its place — flagged for normalization/replacement. WRITTEN REVIEW delivered (cut/add/rationale); spun off the larger work: F15 (add avg-winner/loser, payoff ratio, max-DD % + duration, profit concentration, optional Sortino/streak-$) and CH18 (trim redundant Total Commissions + Account Information rows; normalize or replace Best/Worst Weekday)."
    },
    {
      "id": "R7",
      "title": "Decide what business information the site needs",
      "category": "REVIEW / AUDIT",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Decided what business info the site needs and where it lives. CURRENT STATE: footer + legal.html carry good Disclaimer + Terms-of-Use + a Privacy SUMMARY + contact@blotterbook.com; but the footer has NO copyright line, the operating entity (LICENSE: 'Scaline Audio / Mitchell Guthrie') appears nowhere public, there is no formal Privacy Policy / Terms of Service / refund policy / governing-law clause, and the pricing page already advertises $25/$50 donations + a future paid add-on. OWNER DECISIONS (2026-06): governing law = Delaware; published contact = support email only (contact@blotterbook.com); paid tiers = voluntary NON-REFUNDABLE donations (no goods exchanged); operating-entity display name = TBD. CHECKLIST / PLACEMENT — Have now: not-a-broker/not-advice disclaimers (legal + footer), as-is + liability + proprietary terms (legal), privacy summary (legal), support email (footer/legal). MUST-HAVE BEFORE TAKING MONEY (gate on R15 Stripe): (1) decide + display the operating entity / seller identity (footer '© 2026 <entity>' + a 'Blotterbook is operated by <entity>' line on legal) — Stripe needs a real seller; this is the top blocker. (2) A non-refundable Donations/Refund policy on the legal page (gifts, no goods, non-refundable). (3) A governing-law/venue clause (Delaware) in the Terms. (4) Confirm the support-contact email is clearly reachable from checkout. SHOULD-HAVE soon after / before the paid synced-workspaces add-on: a fuller standalone Privacy Policy (current is a summary) and a proper Terms of Service distinct from the disclaimer bullets; billing/subscription terms when subscriptions ship. NICE-TO-HAVE: footer copyright line regardless of monetization; 'Last updated' dates per legal section. Spun off CH22 (footer copyright/operator line) and F18 (pre-monetization legal pack: ToS w/ Delaware governing law, full Privacy Policy, non-refundable Donations/Refund policy, operator identity — prereq for R15). Review-only; no app code shipped."
    },
    {
      "id": "CH2",
      "title": "Update the contact email to contact@blotterbook.com",
      "category": "CHORE / REFACTOR",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** replaced `contact@scalineaudio.net` → `contact@blotterbook.com` across the marketing pages (index/changelog/howto/roadmap/legal), the app Contact mailto buttons (`app/app.html`, `app/demo.html`), the shared `partials/footer.html`, and `LICENSE` (licensing-inquiry line). mailto subject/body params preserved. The only remaining `contact@scalineaudio.net` is this item's own task description in `data/backlog.json`, left intact as the historical record. NOTE: LICENSE's licensing-inquiry contact was switched to blotterbook.com for consistency with the done-when — revert that one line if licensing should stay on the company domain."
    },
    {
      "id": "CH3",
      "title": "Retire change-plan.md (redundant with the backlog)",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "trivial",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Verified — nothing to remove:** `.claude/change-plan.md` is gitignored and was never committed, so it is absent from the repo and this checkout (confirmed via `git log` — no history). `data/backlog.json` is already the sole committed planning source. No file change was needed; a stray local `change-plan.md` on a dev machine, if any, can be deleted by hand."
    },
    {
      "id": "B3",
      "title": "(staging) Debug why the Activity Terminal module won't collapse properly",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the staging Activity Terminal `.pbody` carried `display:flex` (0,3,0 specificity, same as the generic `.panel.collapsed .pbody{display:none}` but later in source, so flex won). Scoped it to `.panel.term:not(.collapsed) .pbody` (`app/app.css`). Verified: the terminal panel now collapses/expands like every other module."
    },
    {
      "id": "S7",
      "title": "Escape backup-restore symbol/tag option sinks (stored XSS)",
      "category": "SECURITY",
      "priority": "P1",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** two layers. Sink: the f_symbol/f_tag filter <option> interpolations (app/data.js) are now wrapped in esc(). Boundary: Store.importAll() (app/store.js) forces trades[].root through Adapters.rootSym() and strips <>&\"' from trademeta tags, so a restored backup matches CSV-imported data. Verified in Chromium: importing a backup whose symbol is a crafted img/onerror payload does NOT execute (no alert, no live element), the root is stored as sanitized text (EVILIMGSRCXONERROR...), the dropdown renders inert text, and a normal restore still completes."
    },
    {
      "id": "S8",
      "title": "Minor API/security hardening bundle (cookie/compare/headers)",
      "category": "SECURITY",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** (1) the bb_staging admin-token cookie now carries ;Secure; (2) the raw-key fallback in isAdminAuthorized() uses a constant-time compare (HMAC both sides under a random key — leaks neither contents nor length); (3) /api/me is now no-store (via the shared json() helper); (4) dropped the wildcard Access-Control-Allow-Origin from /api/changelog. Verified with a Node WebCrypto test: constant-time accept/reject, me.js no-store, all function modules import."
    },
    {
      "id": "S9",
      "title": "Tighten CSP: drop script-src 'unsafe-inline' (externalize inline scripts)",
      "category": "SECURITY",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** removed the last inline executable scripts so the CSP forbids inline JS. Moved each page's inline <script> to its own file (index->/assets/home.js, admin->/assets/admin.js, changelog->/assets/changelog.js); rewrote the main/demo new-tab Export report to load external /assets/report.js with per-report data in a non-executable <script type=application/json> block and addEventListener-wired buttons (it inherits the app CSP). _headers script-src is now 'self' (no 'unsafe-inline'); style-src keeps it (inline styles remain). Verified in Chromium UNDER THE EXACT DEPLOYED CSP HEADER: homepage feature explorer, admin backlog, the demo new-tab report (downloads a clean HTML file), and the staging export modal all work with ZERO CSP violations; no inline on*= handlers remain in served HTML."
    },
    {
      "id": "S10",
      "title": "Defense-in-depth: rate-limit admin writes + token out of localStorage",
      "category": "SECURITY",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped (defense-in-depth):** added a best-effort KV-backed fixed-window rate limiter (functions/_lib/http.js rateLimited()) keyed by Access email / client IP — /api/status + /api/config at 30/min, /api/admin-key at 20/min, 429 over limit; fails OPEN if STATUS_KV is unbound so it can't lock the admin out. assets/admin.js no longer writes the admin token (or any key) to localStorage — it lives only in the page-session field and autoKey() re-issues a fresh token each load, so an XSS can't lift a persisted credential. Verified with a Node test (35 reqs at limit 30 -> 5 blocked; fail-open without KV)."
    },
    {
      "id": "B4",
      "title": "Commission undercount: charge per contract (× qty), not per trade",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** round-turn commission is now charged per CONTRACT — `2 × per-side rate × (qty||1)` — in `costModel()` and the curve's `dailySeries()`. Close-event exports (no qty) are unchanged; fills/MotiveWave trades with qty>1 are priced correctly. The per-symbol table gained a Contracts column so `$/RT × Cts` reconciles to Commission, and the caveat copy was updated."
    },
    {
      "id": "B5",
      "title": "Non-US date + non-ET timezone assumptions misplace trades",
      "category": "FIXES",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** `normTime()` now detects D/M/Y when the first field is >12 (e.g. 25/06 → Jun 25), defaulting to US M/D/Y otherwise; a new \"US dates & Eastern time assumed\" caveat documents the no-timezone-conversion / ET-session assumptions. Verified by adapter tests (M/D/Y stays US, D/M/Y day>12 swaps)."
    },
    {
      "id": "B6",
      "title": "IndexedDB addTrades transaction auto-commit race on large imports",
      "category": "FIXES",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** `Store.addTrades()` (`app/store.js`) was awaiting a `get()` per trade inside one readwrite transaction, which could auto-commit mid-flight on large imports (TransactionInactiveError). It now reads all ids once into a Set, then issues every `put` in a tight synchronous loop — also dedupes rows repeated within the same batch."
    },
    {
      "id": "B7",
      "title": "Adapter parsing robustness (Schwab truncation, blank rows, flip fills)",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** three adapter-robustness fixes in `app/adapters.js`: `parseCSV` drops all-empty lines; the Schwab adapter skips a row with a blank optional Side cell (`continue`) instead of `break`, only stopping at a real column-count boundary; `pairFills` apportions a flip fill's realized PnL over the closed qty (not the full fill qty) so the full realized value lands on the closed contracts. +5 regression tests (60/60 pass)."
    },
    {
      "id": "B8",
      "title": "Metric display polish (recovery ∞, curve subs accrual, tax weights)",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** Recovery Factor shows ∞ for an all-positive curve with no drawdown (was —); subscriptions now accrue month-by-month along the equity curve instead of dropping at day 0 (endpoint total unchanged); the 1256 tax weights are normalized if loaded `state-tax.json` drifts off the 60/40 split. In `app/core.js` + `app/render.js`."
    },
    {
      "id": "B9",
      "title": "(a11y) Data-manager modal is permanently aria-hidden; no focus management",
      "category": "FIXES",
      "priority": "P1",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the data-manager and staging export modals are now accessible — a shared `modalOpened`/`modalClosed` helper (`app/ui.js`) flips `aria-hidden`, moves focus into the sheet, traps Tab, and restores focus to the opener on close. Escape + backdrop close still work. Verified in Chromium (aria-hidden toggles, focus enters/restores, Escape closes)."
    },
    {
      "id": "B10",
      "title": "(a11y) Keyboard + ARIA for the feature list, calendar, and curve",
      "category": "FIXES",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the homepage feature explorer is now a real tablist (role tab/tabpanel, roving tabindex, Arrow/Home/End with selection-follows-focus, aria-live detail); calendar day cells and the equity-curve SVG are keyboard-operable (tabindex/role=button, Enter/Space; the curve adds arrow-key date navigation), and decorative `.ficon` SVGs are `aria-hidden`. Verified in Chromium (15 keyboard/ARIA assertions)."
    },
    {
      "id": "CH5",
      "title": "Add social/SEO meta (OG, Twitter card, canonical) + OG image",
      "category": "CHORE / REFACTOR",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** generated a 1200x630 raster OG card (assets/og-image.png, branded dark card). index.html gets full Open Graph + Twitter summary_large_image tags + rel=canonical; howto/roadmap/changelog/legal each get rel=canonical + per-page OG/Twitter (own title/description, shared OG image). Shared links now render a rich preview and every public page declares a canonical URL."
    },
    {
      "id": "CH6",
      "title": "Add robots.txt + sitemap.xml",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Audit + shipped.** A Cloudflare-managed robots.txt was already served at the edge (AI Crawl Control: `User-agent:* / Allow:/` + `Content-Signal: search=yes,ai-train=no`, and `Disallow:/` for Amazonbot, Applebot-Extended, Bytespider, CCBot, ClaudeBot, CloudflareBrowserRenderingCrawler, Google-Extended, GPTBot, meta-externalagent; no sitemap). Per decision, that managed file was DISABLED in the Cloudflare dashboard so there's one source of truth. Added a committed root `robots.txt` that subsumes the prior AI-crawler blocks + the `Content-Signal` reservation, and adds `Disallow: /app/` + `/admin.html` and a `Sitemap:` line; plus `sitemap.xml` listing the 5 public pages (matching their CH5 canonical URLs). Verified: sitemap is valid XML and its locs equal the page canonicals; robots carries the disallows + content-signal + sitemap line. NOTE: confirm on the live site post-deploy that only our file serves (the Cloudflare override is off)."
    },
    {
      "id": "CH7",
      "title": "Add CI/automation so the hand-run build tooling can't drift",
      "category": "CHORE / REFACTOR",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** .github/workflows/ci.yml (push to main + PRs) runs test-adapters.cjs, regenerates partials (build-includes) + the data manifest (build-manifest), and FAILS on any uncommitted diff — so stale partials or a stale manifest can't merge. Made build-manifest deterministic for the gate: dropped the generatedAt timestamp (nothing reads it; the app cache-busts the manifest fetch itself) and excluded backlog.json (admin-only, fetched no-store) so the manifest stops churning on backlog edits. Verified: manifest re-run = no diff, 55/55 adapter tests pass, build-includes idempotent. Note: a SessionStart hook isn't committable (.claude/ is gitignored) — the Actions workflow is the enforcement."
    },
    {
      "id": "CH8",
      "title": "Single-source the version string (kill the staging.js literal + drift)",
      "category": "CHORE / REFACTOR",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** app/staging.js reads the version from the .ver page badge instead of a hardcoded 'v0.13' in the activity-terminal 'session ready' line, so a bump no longer touches staging.js. README documents a version-bump checklist (badge = display source; reconcile config.js KV defaults + admin placeholder). Verified: the staging terminal shows 'Staging session ready · v0.13' read from the badge."
    },
    {
      "id": "CH9",
      "title": "Small cleanups: stale CSS breakpoint comment + relative live-pill fetch",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "trivial",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the app/app.css L1 breakpoint-scale comment now references surviving selectors (.uc-row, .feat-explorer) instead of the F7-deleted .grid3/.uc-grid; assets/home.js live-status pill pings /app/ (absolute) instead of the relative app/, matching every other fetch + the _redirects convention."
    },
    {
      "id": "CH10",
      "title": "Extract the duplicated functions json() Response helper",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** new functions/_lib/http.js exports a single json(obj,status,headers) helper (no-store + charset by default). config/status/admin-key dropped their byte-identical local copies; me/checkout/webhook/geo now return via the shared helper for a consistent response shape (also gives /api/me no-store). changelog keeps its bespoke long-cache headers by design. Verified: all function modules import; json() defaults + override correct."
    },
    {
      "id": "A6",
      "title": "Finish A5's deferred tail: app-topbar + app-scripts partials, demo shell",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** finished A5's tail — `app-topbar` and `app-scripts` partials now single-source the three top bars and the ordering-sensitive script block, and demo's trimmed landing + preview-only modal fold into `app-landing`/`app-modal` via a `mode=demo` branch. The three built pages are render-equivalent to before (comment-stripped/whitespace-normalized diff + 27 Chromium assertions); build stays idempotent and 100% static."
    },
    {
      "id": "L5",
      "title": "Theming hygiene: route stray hexes through tokens + AA contrast check",
      "category": "LAYOUT (web vs mobile)",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** stray hex literals routed through `tokens.css` — added `--hover-line` (#33405a hover borders) and replaced on-accent text `#0d1014` with `var(--bg)`. `--faint` was failing WCAG AA on `--bg` (3.18:1) and was bumped #5b6470 → #767f8c (4.71:1); `--dim` (6.22:1) already passed."
    },
    {
      "id": "B11",
      "title": "Large-import crash: Math.max(...pnls) spread overflows the call stack",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** `compute()` (`app/core.js`) now tracks best/worst as running min/max inside the existing equity-curve loop instead of `Math.max(...pnls)`/`Math.min(...pnls)` — the spread overflowed the call stack on large fills exports and blanked the dashboard. Verified a 200k-row dataset no longer throws; metrics unchanged."
    },
    {
      "id": "B12",
      "title": "Unguarded top-level prev/next handlers can kill app boot",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the calendar prev/next handlers route through the null-safe `on()` helper (`app/main.js`) instead of `$('prev').onclick`, so an absent id can't throw at module load and kill `boot()`. Verified prev/next still navigate months."
    },
    {
      "id": "B13",
      "title": "Cost inputs silently coerce blank/NaN to $0, understating costs",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** `numIn()` (`app/core.js`) clamps negatives to 0 (a typed `-5` can't manufacture negative cost) and the platform-fee input gained `placeholder=\"0\"` so an empty field reads as 0 rather than a typed value."
    },
    {
      "id": "B14",
      "title": "TradeStation auto-detect matches at the acceptance floor (over-eager)",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the TradeStation adapter (`app/adapters.js`) now requires the distinctive combined `date/time` column and scores 3 (was 2, the detect floor), so a generic split-time fills export no longer auto-claims as TradeStation. IBKR also has date/time but uses `buy/sell`, so no collision. Regression test added (62/62 pass)."
    },
    {
      "id": "B15",
      "title": "Keyboard focus lost on calendar/curve re-render (B10 follow-on)",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** `renderCalendar()` (`app/render.js`) remembers the focused day's date and restores focus to the same cell after its wholesale innerHTML rebuild, so keyboard users no longer drop to `<body>` on re-render. (The `#curve` SVG element persists across renders, so it never lost focus.) Verified in Chromium."
    },
    {
      "id": "S12",
      "title": "Admin auth / staging gate / ?check fail-open when Access env unset",
      "category": "SECURITY",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** admin-key issuance and the staging gate now **fail closed** when `ACCESS_TEAM_DOMAIN`/`ACCESS_AUD` are unset (admin-key → 503, staging → 403) unless `ALLOW_PRESENCE_AUTH=1` is set for local/preview — closing the spoof-the-header-off-Access path. The `?check` diagnostic is gated behind `ADMIN_DEBUG=1` (off by default) so it can't disclose Access config/email to anonymous callers. Prod (Access configured) is unaffected. README updated."
    },
    {
      "id": "S13",
      "title": "Pre-implementation security checklist for the Stripe/account stubs",
      "category": "SECURITY",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** added `verifyStripeSignature()` (`functions/_lib/auth.js`) — HMAC-SHA256 over the RAW body, constant-time compare, 5-minute replay window, multi-`v1` rotation support — and wired `/api/webhook` to verify before acting (no secret → 501, bad sig → 400, valid → 501 pending provisioning). Reinforced checkout/me with SECURITY notes (server-side price IDs only; authorize the session before a non-local tier). New `scripts/test-auth.mjs` (13 assertions) added to CI. Account provisioning itself stays a scaffold."
    },
    {
      "id": "A7",
      "title": "Single-source the HTML-escape helper and make it quote-safe",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the three duplicated escapers (`esc` in datamanager.js used app-wide, `esc` in changelog.js, `bkesc` in admin.js) collapse into one shared `assets/util.js` loaded first on every page. The shared `esc()` now also encodes `\"` and `'`, closing the latent attribute-injection that only the strict CSP was guarding. Verified across app/changelog/admin."
    },
    {
      "id": "A8",
      "title": "Exported report hardcodes the palette (drifted from tokens.css)",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the exported report's `:root` is generated from the live token values (`getComputedStyle` at export time) instead of a hardcoded palette, and the remaining `#0d1014` literals read from `var(--bg)`/the `--bg` token. The report no longer ships the pre-L5 `--faint:#5b6470` — verified it now carries `#767f8c`, matching tokens.css."
    },
    {
      "id": "A9",
      "title": "Fold the last hand-duplicated nav/footer into the include/token system",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped (documented divergence):** index.html (marketing landing) and admin.html (internal tool) keep genuinely-distinct bespoke nav/footer — homepage uses section anchors + CTA/hamburger and a fuller disclaimer; admin uses a trimmed nav + 'internal' disclaimer. Folding them into the shared partials (or coupling the standalone homepage to site.css) would add risk for no gain, so each file now carries an inline note explaining why it stays bespoke, and the build-includes.mjs header is corrected (it also fixes the now-stale claim that demo keeps a bespoke shell — A6 folded it in)."
    },
    {
      "id": "CH11",
      "title": "Small refactor/perf cleanups surfaced by the R1 audit",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** `costModel(m)` computed once per `renderDash()` and threaded into renderCards/renderAdv/renderCalc (was ~4 walks/render); `downloadFile()` now takes a string OR a Blob (replacing `expDlBlob`); the report's headline figures are defined once and rendered as both HTML tiles and the plaintext summary; journal autosave captures date+text at schedule time (no wrong-date writes on a fast day-switch); stale `var(--red,#e0564d)`/`var(--panel,#11151b)` CSS fallbacks removed; and the CI drift gate now also catches untracked outputs via `git status --porcelain`."
    },
    {
      "id": "F10",
      "title": "(staging) Restyle the 1256 tax row as a collapsible breakdown",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** in staging, the Break-even module now shows one **Est. 1256 tax (net profit only)** headline line with the total, and the State top rate + Blended 1256 rate moved into a collapsible **Tax breakdown** details element (mirrors the F6 commission subsection). Main app + demo keep the inline rows. Values unchanged (gated on STAGING_PAGE in renderCalc)."
    },
    {
      "id": "F11",
      "title": "Day-notes editor visible only when a day is selected; click-outside deselects",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the Day-notes editor (#journal) is hidden until a day is actively selected and reappears on selection; a click outside the Trading Calendar module (and outside the equity curve, the other date-selection surface) deselects the day — clears the highlight, drops the graph marker, hides the box. Capture-phase document handler so a curve re-render cannot detach the target. Verified on app/demo/staging."
    },
    {
      "id": "F12",
      "title": "(staging) Reverse the Activity Terminal so newest is on top",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "trivial",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** the staging Activity Terminal now prepends new lines so the most recent entry is at the TOP; the line cap trims the oldest (now last) and the view scrolls to top. Formatting/cap unchanged."
    },
    {
      "id": "CH12",
      "title": "Automate two-track versioning from conventional commits + merges",
      "category": "CHORE / REFACTOR",
      "priority": "P2",
      "effort": "large",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** one source of truth data/versions.json ({prod, staging}) replaces the two disconnected sources. A CI job (.github/workflows/version-bump.yml -> scripts/bump-version.mjs) bumps on merge to main — level from the conventional-commit type, track from changed paths (shared code -> both, app/staging.* -> staging, non-app -> none) — and commits versions.json back as chore(release): ... [skip ci]. Badges are runtime-fetched (assets/util.js) with the baked partial literal as offline fallback; /api/config + admin show the values read-only (manual inputs + KV versions record retired); phase derived from prod major (0.x -> Beta). versions.json excluded from the manifest; separate concerns (DB_VERSION, backup version, manifest hashes, refDataVersion) untouched. 24 bump-logic unit tests + 7 Chromium assertions pass; added to CI. README versioning section rewritten. Seed prod 0.12.0 / staging 0.22.0."
    },
    {
      "id": "CH13",
      "title": "Drop prompts from Done items; keep only doneNote",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Shipped:** stripped the `prompt` field from all 74 done items (kept only `doneNote`, the shipped record), and added a top-level `donePolicy` next to `commitConvention` stating that setting an item to `done` removes its prompt and keeps the doneNote (prompts stay on open/guardrail items). Net diff: 1 insertion (the policy) + 74 prompt deletions, no other changes. Verified backlog.json is valid JSON and the admin backlog panel still renders all 87 items (74 done · 11 open · 2 guardrail) with no errors — it never rendered done-item prompts anyway."
    },
    {
      "id": "F13",
      "title": "Revamp the changelog into versioned, user-facing release notes",
      "category": "FEATURES",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Discussed and agreed three decisions (curated source / prod track only / manual maintenance), then built. New source of truth: curated, version-keyed data/changelog.json (prod track, newest first) — each release has version, date, friendly title/summary, optional highlights; pre-versioning rolled into one beta:true 'Beta released' entry. assets/changelog.js now fetches the static file and renders versioned timeline cards (version badge, formatted date, Latest pill, summary, highlight bullets) with a small inline fallback; retired the raw-commit functions/api/changelog.js entirely. Seeded from the real chore(release) history: 0.13.0→0.14.2 plus the Beta floor. Added .entry .ver/.summary/.highlights styles to site.css; excluded changelog.json from build-manifest (fetched directly, like backlog/versions). Updated README, legal.html, and changelog.html copy off 'commit history'. Verified in-browser: 5 entries render from the live fetch, no console errors; tests 62/27/13 green, builds idempotent. Future releases: add a top entry to data/changelog.json each prod bump."
    },
    {
      "id": "A10",
      "title": "Define the staging → app/demo promotion workflow",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "**Discussed, then documented.** Agreed three decisions: (1) version model stays independent counters — a promotion bumps prod by the commit-type level and staging keeps its lead, no numeric sync; (2) lives in README as a section + copy-pasteable checklist; (3) demo mirrors prod 1:1, with trade-mutating UI greyed out/disabled and persistence blocked (DEMO_MODE), so a promoted feature MUST appear on demo with its restrictions applied, not be excluded. Added a 'Promoting a feature from staging → prod' subsection to README.md: the surface-by-gate model, the four gate layers (staging.js / STAGING_PAGE runtime guard / inline staging HTML / mode=staging partial conditional) and how to un-gate each, that staging is a proving ground where EVERY feature promotes (the activity terminal, session pill, and workspace templates are features, not dev tools), and an 8-step checklist covering gate removal, demo-restriction preservation, build-includes regeneration, three-surface verification, conventional-commit titling for the CH12 bump, and a changelog (F13) entry."
    },
    {
      "id": "F14",
      "title": "(staging) Make the 5 headline stat cards open metric detail modals",
      "category": "FEATURES",
      "priority": "P2",
      "effort": "large",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Built staging-only (gate on STAGING_PAGE) per the proving-ground model. renderCards (app/render.js) makes the five headline cards role=button/tabindex=0/data-card with an aria-label only on staging; app/demo cards stay plain. A single reusable #cardModal shell (partials/app-modal.html, <!--IF mode=staging-->) is populated per metric by widgets.js openCardModal() + CARD_VIEWS, with inline-SVG helpers (cmCurve/cmHist/cmBars/cmSplit) and aggregators (cmDow/cmSymPf/cmDDMarks) over the active compute() metrics — no new persistence. Net PnL → equity curve + gross→take-home component bars; Win Rate → W/L/BE split + by-weekday + long/short; Profit Factor → gross-profit vs gross-loss + per-symbol PF table; Avg Win/Loss → win & loss distribution histograms; Max Drawdown → equity curve with peak/trough markers. Reuses the .modal/.sheet pattern + B9 modalOpened/modalClosed focus trap; cards are keyboard-operable (Enter/Space, B10) and Escape/click-outside/✕ close. Verified: all five open metric-specific modals with focus moving into the dialog, keyboard open/close works, and app/demo cards remain non-interactive (no data-card, no #cardModal)."
    },
    {
      "id": "L6",
      "title": "(staging) Show the collapse control only when a module is full-width",
      "category": "LAYOUT (web vs mobile)",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Built all-surfaces via a pure CSS media query (no JS) — the web-grid is universal since CH16, so gating to staging would have re-added the body[data-mode=staging] CSS that CH16 removed. In app/app.css's ≥1100px grid block, the chevron is hidden for every panel except the full-width Performance panel, and a non-perf .collapsed panel's body is forced visible (so a panel collapsed on a narrow screen can't get stuck collapsed-with-no-control after the window widens; the term panel keeps its flex body). Below 1100px every panel is single-column/full-width and stays collapsible exactly as before. Verified in-browser: at 1400px the calendar chevron computes display:none while Performance's shows; at 820px the calendar chevron returns."
    },
    {
      "id": "B18",
      "title": "F11: clicking inside a modal deselects the calendar day",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "app/main.js capture-phase deselect guard now also early-returns on `e.target.closest('.modal')`, so interacting with the data-manager or export modal keeps the selected day. deselectDay() (app/data.js) flushes the debounced journal note (clearTimeout(jSaveTimer) + Store.saveJournal) before nulling selectedDate, closing the data-loss window."
    },
    {
      "id": "S14",
      "title": "Admin token leaks via the ?k= staging-launch URL (Referer/history)",
      "category": "SECURITY",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Kept ?k= as the navigation-safe fallback but neutered its exposure: a top-of-file IIFE in assets/util.js (loads first on every page) history.replaceState-strips ?k= immediately on load, and app/staging.html now carries <meta name=\"referrer\" content=\"no-referrer\"> so no subresource fetch leaks the token via Referer. Token no longer lingers in the address bar/history or any Referer header."
    },
    {
      "id": "CH14",
      "title": "Harden the version-bump workflow (race, merge commits, edge cases)",
      "category": "CHORE / REFACTOR",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Hardened both halves of the CH12 automation. version-bump.yml: added `concurrency: {group: version-bump-main, cancel-in-progress: false}`, fetch-depth:0, diffs the pushed range (before..after with a HEAD~1 fallback), and wraps bump→build-includes→commit→push in a 5-attempt retry loop (reset --hard HEAD~1 + pull --rebase on rejection) so close merges can't lose a bump. bump-version.mjs: the local `git diff HEAD~1 HEAD` is now guarded (try/catch → treat as no changes) so it's safe on the first/shallow commit. util.js sets window.__versionsReady = Promise.resolve(null) before its early return so the promise contract always holds."
    },
    {
      "id": "B16",
      "title": "Prod version never bumps on homepage / info-page changes",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Chose the 'what users see at blotterbook.com' intent. Added a PROD_ONLY set (index.html, site.css, howto/roadmap/legal/changelog.html) to classifySurfaces in scripts/bump-version.mjs so those user-facing changes now bump the prod track. Added prod-only surface cases to test-version.mjs (now 27 tests): index.html → prod only, site.css+howto.html → prod only, admin.html/README/backlog → neither, index.html+staging.js → both."
    },
    {
      "id": "B17",
      "title": "Baked .ver fallback literals drift behind the live version",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "bump-version.mjs now calls syncBakedBadges() after writing versions.json: it regex-rewrites the baked `.ver` literals in partials/app-topbar.html (Main app/Demo → prod, Staging → staging), and the workflow runs build-includes and commits partials/app-topbar.html + app/app.html/demo.html/staging.html alongside data/versions.json. The offline fallback now tracks the live version. Synced the current literals to v0.14.1 (Main/Demo) / v0.24.1 (Staging)."
    },
    {
      "id": "S15",
      "title": "Imported screenshot data URLs are unsanitized at the <img src> sink",
      "category": "SECURITY",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Added a cleanShots() sanitizer to importAll (app/store.js) that keeps only strings matching `^data:image/(png|jpe?g|webp|gif);base64,[A-Za-z0-9+/=]+$`, dropping any crafted shot payload at the trust boundary. Defense-in-depth: the editor now escapes the value at the render site — `<img src=\"${esc(s)}\">` (app/datamanager.js). A malformed shot URL is now both dropped on import and inert if rendered."
    },
    {
      "id": "CH15",
      "title": "Wire or clearly disable the dead admin config controls",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Chose the honest-labeling path (no consumer wired yet). The Feature flags section already carried the '(Scaffold — not yet consumed by the main app.)' note; reworded the Reference data section in admin.html to state plainly that the control only records a server-side timestamp in KV and has no client effect — clients cache-bust reference data via per-file hashes in data/manifest.json, not refDataVersion. No admin control now implies an effect it doesn't have."
    },
    {
      "id": "A11",
      "title": "Single-source the platform-phase (0.x→Beta) derivation",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Factored the phase rule into a shared classic helper `platformLabel(prod)` in assets/util.js (loads first on every page); assets/admin.js now calls it instead of its inline re-implementation. Added a 'MIRROR of platformLabel() in scripts/bump-version.mjs — keep in sync' note on the util.js copy so the Node/CI side and browser side stay aligned."
    },
    {
      "id": "B19",
      "title": "Promoted staging widgets use undefined CSS tokens (wrong colors)",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Mapped the undefined CSS tokens in app/app.css's promoted widget blocks to the canonical ones — --muted→--dim, --text→--txt, --field→--panel2 (dropped the #10141a literal fallback). Verified in-browser: the version badge and session pill resolve to #8a94a3 (--dim) and the workspace select background to #1b212a (--panel2); `grep -nE 'var\\(--(muted|text|field)' app/app.css` returns nothing."
    },
    {
      "id": "C2",
      "title": "Stale 'staging-only / staging.js' comments after the CH16 promotion",
      "category": "COMMENTS / READABILITY",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Updated the stale post-CH16 comments that referenced the deleted app/staging.js or labelled promoted features 'staging-only': app/ui.js:5, app/main.js:132 (app:ready) + :97 (saved-filter controls), app/core.js event-bus block, app/data.js:207/:212 (saved filters), app/datamanager.js:68. Each now describes the shared widgets.js / shared saved-filter UI (Save disabled on demo). Left the genuinely staging-only F5 notes (data.js:358, main.js:23) intact."
    },
    {
      "id": "S16",
      "title": "Escape two admin/innerHTML sinks; sandbox the export-report iframe",
      "category": "SECURITY",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Closed three CSP-mitigated escaping/hardening gaps: esc() the admin status mode+label (assets/admin.js:38), the feeds.json optgroup/option labels (app/data.js populateFeeds), and feedName()/broker/state/platform at the two HTML report sinks (render.js cost caption, export.js report header); added sandbox=\"allow-same-origin allow-modals\" to the export-report iframe (#exp_preview). Verified the modal still renders and PNG + Markdown exports download with no console errors."
    },
    {
      "id": "CH17",
      "title": "De-duplicate the export-report cost/summary rows (HTML vs Markdown)",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "De-duplicated the export-report data: the cost/tax breakdown is now a single `costRows` [label,value,valueClass,rowClass] array and the summary reuses the existing `headline` array; the HTML report table and the Markdown download both map them. This fixed the real drift — the Markdown 'Cost & tax breakdown' now includes the 'State top rate' row the HTML had and MD lacked. Verified the .md download still works."
    },
    {
      "id": "B20",
      "title": "Demo activity terminal never logs the initial dataset load",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "runDemo() and restoreSession() now emit a `data:loaded` event ({name,count}); widgets.js subscribes and logs 'Loaded · <name> · N trades'. Verified the demo activity terminal now shows 'Loaded · sample data · 1539 trades' on open (previously it only showed 'Session ready')."
    },
    {
      "id": "A12",
      "title": "Activity-terminal a11y + small widget-coupling cleanups",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-26",
      "partial": false,
      "doneNote": "Activity-terminal polish: removed role=\"log\" from #termwin (kept aria-live=\"polite\") since lines are inserted newest-first (insertBefore), which role=log doesn't expect — documented the choice in the partial. Added a comment flagging that DEFAULT_DASH_ORDER must stay in sync with the data-key order in app-dash.html; hoisted the bare 200 terminal line-cap to const TERM_MAX_LINES; added a note that the 'degraded' session state is reserved/forward-looking (no caller by design)."
    },
    {
      "id": "F15",
      "title": "Advanced Stats — add the metrics traders expect",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Added the metrics traders expect to Advanced Statistics, all from existing data: Avg Winner / Avg Loser ($) + Payoff Ratio (avg win ÷ |avg loss|) under Edge & Distribution; Max Drawdown now shows $ AND peak-relative % AND duration (trades peak→trough); Profit Concentration (% of net profit from the top 5 winners); Sortino (downside-deviation Sharpe) beside the daily Sharpe; and Largest Win/Loss Streak in $ beside the Max Consecutive counts. compute() (core.js) gained maxDDpct/maxDDdur/concPct/sortino/maxWinStk/maxLossStk; renderAdv() (render.js) surfaces them keeping the R5 .avg value-group layout (verified all rows share one right edge, 2 flex children each, no console errors on demo). Definitions panel updated with entries for payoff/avg win-loss, profit concentration, Sortino vs Sharpe, streak $, and the DD %/duration caveat."
    },
    {
      "id": "F16",
      "title": "Unify day & trade notes (tags/screenshots) + show that day's trades list",
      "category": "FEATURES",
      "priority": "P2",
      "effort": "large",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Day notes are now first-class annotations: the JOURNAL record carries {text,tags[],shots[]} (additive — legacy {date,text} backups still import), and the calendar notes block is a quick-capture textarea that expands (<details>) to a tags + screenshots editor reusing the trade editor's pipeline. Autosave keeps a captured-snapshot (CH11) so a debounced/blur save lands on the right day after switching days. A selected day also shows a read-only intraday trades list under the notes, sourced from baseTrades() (respects the active filter bar + scope, per the agreed data-handling decisions) and rendered via the shared trade-row cells. Verified end-to-end (19 checks): rich notes persist + restore, screenshots round-trip, list respects filters, legacy import works; no console errors."
    },
    {
      "id": "CH18",
      "title": "Advanced Stats cleanup — trim redundancy + rework Best/Worst Weekday",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Trimmed Advanced Stats redundancy and reworked the weekday rows. CUT: 'Total Commissions' (duplicated the Break-even panel's Commissions line) and the entire 'Account Information' section (First/Last Trade Date restated the scope range); folded the one useful value, Active Days, into the Trading Patterns subhead so the module no longer ends on plain metadata. REWORKED Best/Worst Weekday: core.js now ranks active weekdays by per-trade AVERAGE PnL (avg=pnl/n), not total — so the demo 'Worst Weekday' reads +$47.22/trade · Thu instead of a misleading +$14.9k total. The raw count is shown alongside (· Mon · 289). Definitions/caveats updated for the new average basis and thin-sample warning. Kept the R5 .avg value-group layout (verified consistent)."
    },
    {
      "id": "CH19",
      "title": "Extract a shared annotation-editor component (day + trade notes)",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Extracted a shared annotation editor in datamanager.js (annTagsField / annTextField / annShotsField / annCapture / annAddShot) used by BOTH the per-trade editor and the per-day editor; the screenshot downscale/encode + Store.validShot pipeline now lives in one place. dmCaptureEdit/dmAddShot are thin wrappers so existing callers are unchanged; DM_EDIT note→text internally, mapped to `note` only at the Store boundary."
    },
    {
      "id": "CH20",
      "title": "Manage-data Day-notes rows: open/edit + consistent markers",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Manage-data Day-notes rows now show the same tag/image markers as trades (journalChips) and gain an 'Open' button (dmOpenDay) that closes the modal, jumps the calendar to that date, selects it, and scrolls to the notes — reusing the shared editor. Verified the row markers + Open-selects-day."
    },
    {
      "id": "B21",
      "title": "(staging) Remove the blue selection outline on the performance graph",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Clicking a day focuses the curve SVG (tabindex=0), which painted the UA focus ring around the whole graph. Added .curve:focus{outline:none} before the existing .curve:focus-visible accent rule, so mouse-click focus shows no box while keyboard focus keeps a 2px accent ring (ordering makes :focus-visible win when both match). Verified in-browser: mouse click → outline none, real Tab focus → solid 2px ring."
    },
    {
      "id": "B22",
      "title": "(staging) Hide Save-layout button + layout dropdown until the dashboard loads",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Added #wsbar (the Save-layout button + workspace-template dropdown) to the body:not(.loaded){display:none} set in app.css, so the workspace controls only appear once the dashboard loads, matching #exportBtn/#manageBtn/#filterbar/#cards. Verified #wsbar is display:none pre-load and visible after."
    },
    {
      "id": "B23",
      "title": "(demo) Disable the Save-layout button under the demo lock-down",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Disabled #ws_save in demo (PAGE_MODE==='demo') in initWidgets and made its click handler a no-op there — Save layout persists templates to localStorage and the demo is a 1:1 mirror with mutating actions disabled. Loading templates / '— Default —' still work. Verified disabled on demo, enabled on the app."
    },
    {
      "id": "CH21",
      "title": "(admin) Default the backlog filter to status = open",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "The admin backlog now opens filtered to status=open (the actionable view) by default — set in loadBacklog after the options populate, only while the filter is at its initial 'All'; All/done/guardrail stay selectable and the per-category counts still reflect full totals. Verified the select defaults to 'open' and the list shows only open items."
    },
    {
      "id": "R9",
      "title": "(admin) Review the Reference Data section + 'bump cache version' button",
      "category": "REVIEW / AUDIT",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Reviewed the admin Reference Data section + 'bump cache version' button. FINDING: nothing consumed refDataVersion — it only wrote a KV timestamp displayed in admin; real reference-data cache-busting is per-file hashes in data/manifest.json. A prior item had already honest-labeled it as a no-op. DECISION: remove it. Dropped the button + section (admin.html / assets/admin.js) and the refDataVersion/bumpRefData handling from functions/api/config.js. No follow-ups needed."
    },
    {
      "id": "F17",
      "title": "(admin) Add more feature flags and wire them up",
      "category": "FEATURES",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "The app now fetches /api/config at boot (loadFlags, non-blocking) and gates real behaviour on flags, with app-side defaults mirroring config.js DEFAULTS.flags so behaviour is unchanged offline/static/pre-deploy. Wired showBetaAdapters (manual upload picker), maintenanceBanner (top-of-app banner), and a new betaRibbon flag ('Beta' header badge); new flags are allow-listed in DEFAULTS.flags and exposed as admin toggles. Verified both ways: defaults preserve current behaviour; flags from /api/config toggle the banner, ribbon, and beta-adapter picker."
    },
    {
      "id": "R10",
      "title": "Audit the README for accuracy and tighten it",
      "category": "REVIEW / AUDIT",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Audited README.md against the current codebase and corrected the staleness that had accumulated (load order omitting widgets.js; /api/config described as carrying refDataVersion/platform versions after R9 removed them; the ?k= staging-token fallback removed by S19; app/staging.js → widgets.js after the CH16 rename; 'three stores' → four incl. trademeta; six → seven Manage-data sections incl. Saved filters; day notes now rich {text,tags,shots} + a per-day trades list; Advanced Stats metric list F15/CH18; web-grid + saved filters promoted, so roadmap 'shipped' not 'in trial'; homepage pricing Free + donation, not a '$49/mo online app'). Filled genuine Claude-context gaps: the full scripts/test suite + bump-version, and the key one — that build-includes ALSO assembles app/{app,demo,staging}.html from partials/app-*.html via <!--IF mode=…--> conditionals. Tightened by condensing the verbose admin Cloudflare-Access setup (detail lives in functions/README.md). Net result: accurate, with the surface-build + versioning + load-order context Claude relies on intact. README is a no-bump file."
    },
    {
      "id": "B24",
      "title": "Import: num() mis-parses European decimal commas",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "num() now detects thousands-vs-decimal separators and parses EU locale numbers ('1.234,50' → 1234.5) while preserving US '1,234.50'/'1,234' thousands. Tests added (test-adapters.cjs)."
    },
    {
      "id": "B25",
      "title": "Import: same-second fills pair by export order, can flip PnL sign",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "pairFills detects a newest-first export and inverts the row-index tiebreak, so same-second fills pair in execution order instead of CSV order (which could flip a round-trip's long/short + PnL sign). Test added."
    },
    {
      "id": "B26",
      "title": "Import: per-row D/M/Y detection can mix date formats within one file",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Date order (M/D/Y vs D/M/Y) is decided once per file by detectDateOrder() in parse() instead of per-row, so a uniform D/M/Y export no longer mixes formats when only some days exceed 12; standalone normTime keeps the per-value heuristic. Test added."
    },
    {
      "id": "B27",
      "title": "Large-dataset crash: Math.min/max spread reintroduced in widgets.js + render.js",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Replaced Math.min(...arr)/Math.max(...arr) on per-trade arrays (render curve domain, cmCurve, cmHist) with a shared running minMax() helper in core, removing the call-stack-overflow risk on large fills exports that compute() already avoids."
    },
    {
      "id": "S17",
      "title": "Restore: validate & escape trade/journal date on backup import",
      "category": "SECURITY",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Store.importAll now requires canonical YYYY-MM-DD dates (+ finite pnl) on restored trades and journal records, matching the CSV path; the data-manager date sinks are esc()'d. Verified in-browser: a backup with a markup 'date' is rejected on import and the lists render no raw <img>."
    },
    {
      "id": "S18",
      "title": "Client hardening: screenshot data-URL parity + drop style-src 'unsafe-inline'",
      "category": "SECURITY",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Lifted SHOT_RE to a shared Store.validShot() and enforce it on the live screenshot path (dmAddShot) so SVG/non-base64/decode-failure URLs are rejected for parity with restore (verified validShot accepts png, rejects svg + javascript:). Documented the path to drop style-src 'unsafe-inline' in _headers."
    },
    {
      "id": "S19",
      "title": "Server hardening: drop ?k= admin-token-in-URL + allow-list/cap config flags",
      "category": "SECURITY",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Dropped the ?k= staging-token query fallback (admin.js carries the token only in the path-scoped cookie; _middleware no longer reads searchParams). /api/config now allow-lists flag writes to DEFAULTS.flags and boolean-coerces them, so no arbitrary/oversized keys reach the world-readable config record. Auth suite 13/13."
    },
    {
      "id": "CH23",
      "title": "De-duplicate view-layer math (DOW buckets, drawdown walk, commission rows)",
      "category": "CHORE / REFACTOR",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Single-sourced drifted view math: dowBuckets() in core (compute + cmDow), curve-space ddPeakIdx/ddTroughIdx exposed from compute so the drawdown card marks peak→trough without re-walking (cmDDMarks removed), and a shared commSymRows(c,report) for the cost panel + export report (which regained the lost Cts column). Verified the dd card still renders both marks and the win-card DOW."
    },
    {
      "id": "CH24",
      "title": "Dead-code + version-drift cleanup from the R1 audit",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Removed dead/drift code from the R1 audit: unused entitlements.js <script> tag (file kept for the cloud tier), renderAdv's unused cost-model param, the curveMetrics() alias (inlined), and the unused schwab cPos lookup; unified the trade-id accessor on tradeKey() in datamanager; and explicitly documented the changelog fallback as intentionally-unsynced. Build idempotent; tests green."
    },
    {
      "id": "A13",
      "title": "Route layout/workspace localStorage through a single persistence seam",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Added Store.local — the single synchronous persistence seam for UI layout/workspace state — and routed ui.js (panel order/collapse) and widgets.js (workspace templates) through it, so no app/*.js touches localStorage directly. Kept synchronous so layout applies before paint. Verified layout order persists across reload via the seam."
    },
    {
      "id": "B28",
      "title": "Pending day-note autosave can resurrect a note after Erase-all/reset",
      "category": "FIXES",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Added cancelDaySave() (clears jSaveTimer without persisting) and call it in resetApp() and before Store.purge() in the Erase-all handler, so a pending debounced day-note autosave can no longer resurrect a note row into a just-cleared/purged store. Verified in-browser: erasing during a pending edit leaves the store empty."
    },
    {
      "id": "B29",
      "title": "Day-note text nits: preview entity-split + restore tag case",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "(1) The Manage-data day-note preview now slices BEFORE escaping — esc((j.text||'').slice(0,120)) — so truncation can't split an HTML entity. (2) Backup restore now lowercases + dedupes tags (cleanTags) for both journal and trade meta, matching the live editors' canonical form so restored tags hit the tag filter/chips. Verified restored mixed-case/dup tags normalize to lowercase+deduped."
    },
    {
      "id": "CH25",
      "title": "Doc + cosmetic sweep: load-order comments (widgets.js) and day-editor handler consistency",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Load-order header comments in all 7 app/*.js and the core.js orientation block now include widgets.js (real order: …datamanager → widgets → main). Aligned the day-editor blur handler's DAY_EDIT gate with its input sibling, and dmOpenDay's METRICS_ALL-falsy fallback now syncs calYear/calMonth so the selected-day highlight is visible."
    },
    {
      "id": "A14",
      "title": "Guard the client↔Worker feature-flag default drift",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Added scripts/test-flags.mjs (wired into CI) asserting that app/data.js APP_FLAGS and functions/api/config.js DEFAULTS.flags share the same flag keys AND default values — so the client↔Worker flag-default literals can't silently drift. Fails if a flag is added/changed in only one place. 5/5 passing."
    },
    {
      "id": "R16",
      "title": "Review & plan: repo/folder structure for maintainability",
      "category": "REVIEW / AUDIT",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Reviewed the repo/folder structure against best practice for a build-stepless Cloudflare Pages static site. KEY FINDING: because there is NO build step, the repo root IS the deploy/web root — the folder layout is simultaneously the public URL structure AND the deploy contract (measured coupling: 23 absolute /app/ refs, 19 /assets/ refs, /data/*.json + /api/* fetches, _redirects/_headers/robots.txt/sitemap.xml + page canonicals, and hardcoded path prefixes in build-includes ['.', 'app'], build-manifest data/* filename excludes, and bump-version's prod-shipping classifier). So the textbook '/src vs /public with a bundler' reorg FIGHTS the architecture: it either changes every public URL (canonical/sitemap/_redirects/bookmark churn) or requires a build/copy step that violates the 'no build step / deploys as static files' design pillar. CONCLUSION: keep the current root=web-root layout — it is the idiomatic, correct structure for this deploy model (functions/ pinned at root as Pages requires; _headers/_redirects/robots/sitemap/favicon at root; concerns co-located in app/ partials/ data/ assets/ scripts/ functions/; tokens+partials already single-sourced via A1/A5/A6). CONSIDERED & DECLINED as not worth the churn: scripts/->tests/ split (cosmetic; CI path coupling), assets/ -> img+js split (changes ~19 URLs + favicon/OG/_headers refs), data/ subgrouping (breaks fetch URLs + manifest keys), marketing pages -> /site (SEO/canonical/sitemap churn). The ONE Pages-compatible reorg, should the root ever genuinely need it, is a public/ output-dir split (browser-served files under public/, Pages build-output-directory=public, keep functions/+scripts/+partials/+tooling at root) — URLs stay identical, but _headers/_redirects relocate into public/ and every build-script path prefix changes; deferred (cost > payoff for a small project now). SPUN OFF: A18 (guardrail — repo layout is the deploy contract; reject a /src+bundler reorg, and if ever reorganizing use the public/ output-dir approach + update the full coupled-path set in lockstep) and CH28 (document the build-stepless layout contract + path-coupling map in docs/architecture.md). Review-only; no code shipped."
    },
    {
      "id": "CH28",
      "title": "Document the build-stepless layout contract + path-coupling map (docs/architecture.md)",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Documented the build-stepless layout contract. Added a 'Repository layout & the deploy contract' section to docs/architecture.md (after Design pillars; registered in the Contents list): no build step -> the repo root IS the Cloudflare Pages web root -> a file's path IS its public URL. Lists what Pages pins at the deploy root (functions/, _headers, _redirects, robots.txt, sitemap.xml, favicon, and the /-serving pages) and a coupled-path table that must change in lockstep if any browser-served file moves: the ~23 /app/ + ~19 /assets/ absolute refs and /data//api fetches, _redirects, _headers (CSP connect-src), robots/sitemap + page canonicals (CH5/CH6), build-includes.mjs (['.', 'app'] scan + partials/), build-manifest.mjs (data/* hashing + filename excludes), and bump-version.mjs (prod-shipping path classifier). States: do NOT reorganize into src/+public/ with a bundler (violates the no-build pillar); the only Pages-compatible reorg is a public/ output-dir split. CLAUDE.md's Repo layout section gained a blockquote summary + link to the new section (refs guardrail A18). Docs only; no code/build impact."
    },
    {
      "id": "S20",
      "title": "Sanitize the meta store on backup restore (savedFilters id + arbitrary keys)",
      "category": "SECURITY",
      "priority": "P2",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Hardened backup restore (app/store.js importAll). The meta store was restored verbatim; now allow-lists meta keys (savedFilters, setup) and validates the savedFilters shape via cleanSavedFilters — id coerced to [A-Za-z0-9] (<=32 chars), name + filter fields (from/to/symbol/side/session/tag) stripped of [<>&\"'], dows filtered to ints 0-6, and empty-id entries + unknown keys dropped — plus esc(s.id) added at the two render sinks (app/data.js f_saved <option>, app/datamanager.js data-filter* buttons) as defense-in-depth. Closes the backup-driven attribute-injection class S7 fixed but missed on the meta path (the strict CSP already blocked the inline-script path). Verified the sanitizer against a crafted payload: id '><img src=x onerror=alert(1)> -> imgsrcxonerroralert1, markup stripped from name/f, dows [1,'x',9,3] -> [1,3], unknown keys + empty-id dropped. node --check + full test suite green."
    },
    {
      "id": "CH29",
      "title": "Single-source the remaining view-layer cost/metric math (winRate, bePer, curve costModel)",
      "category": "CHORE / REFACTOR",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Single-sourced the remaining view-layer cost/metric math (continues CH23/CH11). Added winRate to compute()'s return (was inlined as (m.n?100*m.wins/m.n:0) in render.js renderCards, export.js headline, widgets.js win modal); added bePer to costModel()'s return (was duplicated in render.js renderCalc + export.js); and threaded the shared cost model c from renderDash into renderCurve -> dailySeries (which had been recomputing costModel(m) every render, bypassing the CH11 share). Pure refactor, no output change. Verified headless in Chromium: the demo dashboard renders Win Rate 58.0% (892W/647L = 57.96%), break-even $2.03/trade, all 5 headline cards, zero NaN/undefined/JS errors."
    },
    {
      "id": "A19",
      "title": "Add a state-tax-model drift test (core.js TAXMODEL fallback vs state-tax.json)",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Added scripts/test-tax.mjs (wired into CI as 'State-tax model default drift test') asserting the app/core.js TAXMODEL fallback literal {fedOrdinary,ltcg,ltcgWeight,ordinaryWeight} equals data/state-tax.json model — mirroring A14's test-flags.mjs guard and closing the one duplicated client default the flag test didn't cover (the literal is the offline/pre-deploy fallback overlaid by Object.assign on fetch). 6/6 passing; fails if the literal and JSON diverge."
    },
    {
      "id": "B31",
      "title": "(demo) Defense-in-depth: runtime-guard the data-manager mutation handlers",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Demo defense-in-depth: added early if(PAGE_MODE==='demo') return; guards to the data-manager mutation handlers in app/datamanager.js (dmSaveEdit, dmClearEdit, dmDeleteTrade, dmDeleteNote, dmImport), so the demo lock-down no longer relies solely on the buttons rendering disabled — demo trades are in-memory, so bypassing disabled via devtools would otherwise operate on the real IndexedDB. dmExport is read-only and left unguarded. node --check + tests green."
    },
    {
      "id": "B32",
      "title": "Harden remaining unguarded DOM handlers (boot init + home.js) — B12 class",
      "category": "FIXES",
      "priority": "P3",
      "effort": "small",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Hardened the remaining unguarded boot-time DOM handlers (B12 class). Routed the direct document.getElementById('f_reset'/'c_feed'/'c_tv').addEventListener calls in app/data.js initFilters/initSetup through the null-safe on() helper (consistent with the adjacent on('f_save') and bind() usage), so a missing id can no longer throw and abort the rest of boot(). Wrapped assets/home.js in an IIFE (no global leakage, matching admin.js/changelog.js) and guarded the #hdr scroll handler with if(hdr). node --check green; demo/app boot clean, homepage unaffected."
    },
    {
      "id": "L7",
      "title": "(staging) Enable F14 stat-card detail modals on mobile",
      "category": "LAYOUT (web vs mobile)",
      "priority": "P2",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Verified ALREADY RESOLVED — no code needed. L7 was filed while F14 (stat-card detail modals) was staging-only and predated/coincided with the CH16 promotion. Current state: the modal interaction ships on ALL surfaces — renderCards (app/render.js) marks the 5 headline cards .card.clickable[data-card], and widgets.js initWidgets binds a DELEGATED click handler on #cards (plus an Enter/Space keydown), with NO hover/pointer/matchMedia gating. A mobile tap produces a click event, so the delegated handler fires on touch. CSS is mobile-ready: .card.clickable has cursor:pointer + :active feedback and the cards stack to one column at <=560px; the .modal overlay scrolls (overflow:auto), the .sheet is width-responsive (max-width + 16px side padding), and the chart SVGs are svg.cmcurve/cmhist{width:100%;height:auto} so they scale without overflow. #cardModal ships via partials/app-modal.html on every surface. VERIFIED end-to-end via the Chromium DevTools Protocol at a 390x844 phone viewport with touch emulation (demo.html): tapping the Net PnL card opened its modal (opened=true); the sheet measured 358px wide in the 390px viewport with overflowsX=false, the overlay overflowY=auto, and the chart scaled to 312px; the screenshot showed the full modal (stat tiles + cumulative-PnL curve + gross->net->take-home waterfall) rendering cleanly with no clipping. The 'Done when' (tapping a headline stat card on mobile opens its detail modal, sized/scrollable for small screens) is already met. Closed with no code change; the stale '(staging)' title label is moot on archival."
    },
    {
      "id": "A21",
      "title": "Move dynamic styling out of inline style= into CSS classes + custom properties",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P3",
      "effort": "medium",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": true,
      "doneNote": "Moved dynamic styling out of inline style= into CSS classes + custom properties (set via the CSSOM), eliminating all runtime data-driven style= from the chart/bar renderers. Converted: the equity-curve gradient stops -> SVG presentation attributes (stop-color/stop-opacity); the curve end-labels -> class .endlab + --c via CSSOM, with a .curve text.endlab{fill:var(--c)} rule that beats the blanket .curve text{fill:var(--dim)} (a bare fill= attribute would have LOST to it — the specificity-inversion trap, caught + handled); the curve hover-tooltip swatch, the card-modal bars (cmBars .cmbf + cmSplit .cmseg), and the admin backlog bars (.bkbar i) -> class + data-w/data-c with a post-insert CSSOM pass (el.style.setProperty('--w'/'--c')) at each render site. Added matching var(--w)/var(--c) CSS in app/app.css + admin.html. VERIFIED zero visual regression: headless-Chromium before/after screenshots of the demo dashboard (curve gradient + end-labels), the curve tooltip, the Net PnL modal (cmBars waterfall), the Win Rate modal (cmSplit), and the admin backlog bars are BYTE-IDENTICAL; node --check + full test suite + build-drift gate green. DEFERRED (partial — out of the chart/bar-renderer done-when, lower value + higher cost): the ~23 static style= attributes in committed HTML, the few static style= in data.js/datamanager.js/main.js, and the large inline <style> blocks in index.html/admin.html (externalizing those adds FOUC + reverses A9 + a new CSS-file URL under the deploy contract — better paired with the R19 build decision). Also a prerequisite step toward dropping style-src 'unsafe-inline' later (currently shelved)."
    },
    {
      "id": "A22",
      "title": "Targeted DOM updates on interactive surfaces (retire wholesale innerHTML re-render)",
      "category": "ARCHITECTURE / DUPLICATION",
      "priority": "P3",
      "effort": "large",
      "status": "done",
      "completedDate": "2026-06-27",
      "partial": false,
      "doneNote": "Verified ALREADY SATISFIED — no code change needed. The prompt's premise (the interactive renderers 'rebuild whole subtrees via innerHTML on every change') was an overstatement; prior incremental work already implemented targeted updates. Verified via Chromium DevTools-Protocol interaction tests on the live app: (1) CALENDAR selection is already targeted — selectDay/deselectDay (app/data.js) toggle the .selday class on the specific cell and persistDay toggles .hasnote on one cell, with NO renderCalendar() rebuild; CDP test confirmed focusing cell X then selecting cell Y keeps focus on X, leaves the grid DOM-node identity unchanged (no rebuild), and gives Y .selday. (2) CURVE keeps interactive state — the #curve SVG element persists across renders (only its innerHTML is replaced); CDP test confirmed focusing the curve then toggling a Gross/Net/Take-home overlay (a full renderCurve) keeps focus on the same SVG node. (3) DAY-NOTE editor edits survive re-render — code-verified that no re-render path touches the note textarea: renderDash -> renderDayTrades writes only #j_trades (a separate container), renderCalendar only toggles #journal's display, and updateJournalEditor (which does rebuild #j_note) runs only on an intentional day-switch / data-load, where CH11's capture-snapshot persists the outgoing day first. (Not runtime-tested on demo since note editing is disabled there, but the render/editor separation is unambiguous in code.) The remaining wholesale innerHTML rebuilds (full renderCalendar grid, renderCurve svg) happen only on genuine DATA/MONTH/OVERLAY changes where a rebuild is correct. The B15 focus-restore in renderCalendar therefore STAYS — it still guards the genuine same-month data-reload rebuild (import/delete) and isn't removable without a riskier grid-diffing rebuild that isn't worth it. Behavioral done-when met with no change; the 'remove B15' sub-clause rested on the inaccurate premise and is intentionally left as-is. Closed like L7/CH3 (verified-resolved)."
    }
  ]
}

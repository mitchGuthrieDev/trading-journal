/// <reference types="vite/client" />
// Ambient declarations for Vite asset imports — notably side-effect CSS imports like
// `import './styles/tailwind.css'` (A128) — so svelte-check resolves them. Vite owns these types.
